from django.contrib import admin
from .models import UserInfo
# Register your models here.
# @admin.register(UserInfo)
# class RegisterUserInfo(admin.ModelAdmin):
#     list_display = ['name', 'roll', 'city', 'is_active']